import SwiftUI

struct SayacView: View {
    var body: some View {
        NavigationStack {
            Text("Sayaç ekranı — bkz. PLAN.md Bölüm 7.1")
                .navigationTitle("Sayaç")
        }
    }
}

#Preview {
    SayacView()
}
